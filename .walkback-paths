# Pinned 2026-09-02 (clib vendoring, see SESSION_LOG 2026-09-02 probe C2).
#
# Upstream AutoHook consumes croizat.clib as a net10-only NuGet package. The TC tree vendors
# a net9-retargeted clib source tree (18 leaf files Compile-Removed) plus a global.json that
# selects the .NET 10 SDK compiler for C# 14 `extension` blocks. Neither exists upstream, so a
# refresh must not touch them; and AutoHook.csproj carries the clib/FishSolver ProjectReferences
# and the TC-managed <Version>, which csproj take-theirs would silently drop the first time
# upstream edits its own csproj.
AutoHook/AutoHook.csproj
clib/*
global.json

# Pinned 2026-09-07: AutoHook vendors ECommons **3.2.1.15** (the second canon, see memory
# project_ecommons_3_2_1_15_second_canon). It has no doc/SUBMODULE_REGISTRY.md row, so the
# source-transform exclusion list was empty and the `using ECommons.GameHelpers.LegacyPlayer;`
# / `using Dalamud.Game.Player;` drop rules -- written for walk-back ECommons 3.0.0.7, where
# those namespaces do not exist -- ran over the vendored 3.2.1.15 tree and stripped them from
# GameFunctions/{FakeParty,PlayerFunctions}.cs and GameHelpers/{Player,LegacyPlayer}.cs
# (CS0103 'Player' + CS1929 GetJob). Identical to the 2026-08-30 ICE failure; same remedy.
# Upstream consumes ECommons as a NuGet package, so there is nothing upstream for this pin to
# fence off at merge time -- its whole job is to fence off the SOURCE transforms.
ECommons/*
